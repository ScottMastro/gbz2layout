# build artifacts
/build/
/local/

# local run data: GBZ inputs, layout outputs, benchmark logs
/data/
/out/

# redundant dep source copies — the headers actually compiled live in
# src/third_party/ (committed). deps/vendor/ is just the upstream clones.
/deps/vendor/

# editor / OS
*.o
*.a
*.so
*.png
*.lay.tsv
*.links.tsv
.DS_Store

# external reference clones (odgi GPU source, read-only)
/reference/
